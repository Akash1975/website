<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= (about && about.title) ? about.title : 'About Me' %> - Akash Dhaigude
  </title>
  <link rel="shortcut icon" href="/uploads/download.jpeg" type="image/x-icon">
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&family=Dancing+Script:wght@400;700&display=swap"
    rel="stylesheet">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      font-family: 'Poppins', sans-serif;
      background: url('/uploads/Background.jpg') center/cover no-repeat;
    }

    main {
      flex: 1;
    }

    /* Navbar */
    nav {
      padding: 10px 20px;
    }

    .nav-list {
      list-style: none;
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
      gap: 20px;
    }

    .nav-list a {
      color: white;
      text-decoration: none;
      padding: 8px 12px;
      font-weight: 600;
      border-radius: 4px;
      transition: background 0.3s ease;
      display: block;
    }

    .nav-list a:hover {
      background: rgba(255, 255, 255, 0.2);
      text-decoration: underline;
    }

    .nav-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .nav-toggle {
      display: none;
      font-size: 28px;
      background: none;
      border: none;
      color: white;
      cursor: pointer;
    }

    /* Mobile Navbar */
    @media (max-width: 768px) {
      .nav-toggle {
        display: block;
        z-index: 1001;
      }

      .nav-list {
        position: fixed;
        top: 0;
        left: -250px;
        width: 250px;
        height: 100%;
        background: rgba(0, 0, 0, 0.7);
        flex-direction: column;
        align-items: flex-start;
        padding-top: 60px;
        padding-left: 20px;
        gap: 15px;
        transition: left 0.3s ease-in-out;
        z-index: 1000;
      }

      .nav-list.show {
        left: 0;
      }

      .overlay {
        display: none;
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.5);
        z-index: 999;
      }

      .overlay.show {
        display: block;
      }
    }

    /* About Section */
    .about-section {
      position: relative;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 60px 20px;
    }

    .about-container {
      position: relative;
      z-index: 2;
      max-width: 900px;
      width: 100%;
      display: flex;
      flex-wrap: wrap;
      gap: 40px;
      justify-content: center;
      align-items: flex-start;
    }

    .about-image {
      flex: 1 1 300px;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .about-img {
      width: 250px;
      height: 250px;
      border-radius: 50%;
      object-fit: cover;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5);
      animation: fadeIn 1s ease forwards;
    }

    .about-content {
      flex: 2 1 400px;
      color: #fff;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(12px);
      border-radius: 20px;
      padding: 30px;
      box-shadow: 0 15px 35px rgba(0, 0, 0, 0.25);
    }

    .animated-title {
      font-size: 2.5rem;
      margin-bottom: 20px;
      color: #f0c040;
      opacity: 0;
      transform: translateY(-30px);
      animation: slideDown 1s ease forwards;
    }

    .animated-text {
      font-size: 1.1rem;
      margin-bottom: 15px;
      line-height: 1.6;
      opacity: 0;
      transform: translateY(30px);
      animation: slideUp 1s ease forwards;
    }

    .animated-text:nth-of-type(1) {
      animation-delay: 0.2s;
    }

    .animated-text:nth-of-type(2) {
      animation-delay: 0.5s;
    }

    .animated-text:nth-of-type(3) {
      animation-delay: 0.8s;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: scale(0.9);
      }

      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(50px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes slideDown {
      from {
        opacity: 0;
        transform: translateY(-50px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Skill Badges */
    .skills-badges {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 20px;
    }

    .skill {
      background: rgba(255, 255, 255, 0.1);
      color: #fff;
      padding: 8px 15px;
      border-radius: 25px;
      font-weight: 500;
      opacity: 0;
      animation: badgeFadeIn 0.5s forwards;
    }

    .skill:hover {
      transform: scale(1.1);
      box-shadow: 0 4px 15px rgba(255, 255, 255, 0.3);
      cursor: pointer;
    }

    @keyframes badgeFadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Popup */
    .popup {
      display: none;
      position: fixed;
      z-index: 1000;
      inset: 0;
      background-color: rgba(0, 0, 0, 0.6);
      animation: fadeIn 0.3s ease-in-out;
      color: white;
    }

    .popup-content {
      background-image: url('/uploads/popUp.avif');
      background-position: center;
      background-size: cover;
      margin: 10% auto;
      padding: 30px;
      border-radius: 15px;
      width: 80%;
      max-width: 500px;
      text-align: center;
      color: white;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }

      to {
        opacity: 1;
      }
    }

    /* Footer */
    footer {
      background: linear-gradient(135deg, #1e1e1e, #333);
      color: #fff;
      text-align: center;
      padding: 25px 15px;
      margin-top: auto;
      box-shadow: 0 -3px 10px rgba(0, 0, 0, 0.2);
    }

    footer .social-icons>a {
      text-decoration: none;
      color: #fff;
      gap: 30px;
      margin: 10px;
    }

    footer .social-icons>a:hover {
      color: #f0c040;
    }

    footer .footer-content {
      max-width: 800px;
      margin: 0 auto;
    }

    footer h3 {
      font-size: 1.3rem;
      color: #f0c040;
    }

    footer small {
      display: block;
      margin-top: 15px;
      font-size: 0.8rem;
      color: #888;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .about-container {
        /*flex-direction: column;*/
        gap: 20px;
      }

      .about-img {
        width: 150px;
        height: 150px;
      }

      .about-content {
        padding: 15px;
      }
    }
  </style>
</head>

<body>
  <div class="overlay" id="overlay"></div>
  <nav>
    <div class="nav-header">
      <button class="nav-toggle" id="navToggle">&#9776;</button>
    </div>
    <ul class="nav-list" id="navList">
      <li><a href="/">Home</a></li>
      <li><a href="/about">About</a></li>
      <li><a href="/contact">Contact</a></li>
      <li><a href="/skills">Skills</a></li>
      <li><a href="/certificate">Certificates</a></li>
      <li><a href="/project">Projects</a></li>
    </ul>
  </nav>

  <!-- About Section -->
  <section class="about-section">
    <div class="about-container">
      <div class="about-image">
        <!-- <img src="/uploads/Akya.jpg" alt="Akash Dhaigude" class="about-img"> -->
        <!-- <img class="about-img" src="/uploads/<%= about.image %>" alt=""> -->
        <img class="about-img popup-trigger" src="/uploads/<%= about.image %>" alt="" style="cursor: pointer;">


      </div>
      <div id="popup" class="popup">
        <div class="popup-content">
          <span class="close">&times;</span>
          <img src="/uploads/Akya.jpeg" width="100" height="100" style="border-radius: 10%;">
          <h2>Akash Dhaigude</h2>
          <p>I am a passionate Full Stack Web Developer with expertise in HTML, CSS, JavaScript, Node.js, MongoDB,
            React.js
            and more!</p>
        </div>
      </div>
      <div class="about-content">
        <h2 class="animated-title">
          <%= (about && about.title) ? about.title : 'About Me' %>
        </h2>
        <p class="animated-text">
          <%= (about && about.paragraph1) ? about.paragraph1 : '' %>
        </p>
        <p class="animated-text">
          <%= (about && about.paragraph2) ? about.paragraph2 : '' %>
        </p>
        <p class="animated-text">
          <%= (about && about.paragraph3) ? about.paragraph3 : '' %>
        </p>

        <h3 style="color: #9b5de5;">My Skills</h3>
        <div class="skills-badges">
          <% (aboutSkills || []).forEach(function(s, idx){ %>
            <span class="skill" style="animation-delay:1 + (idx * 0.1)s;">
              <%= s.skill %>
            </span>
            <% }) %>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <div class="footer-content">
      <h3>Akash Dhaigude</h3>
      <p>Showcasing my hard-earned achievements and dedication to continuous learning.</p>
      <div class="social-icons">
        <a href="https://www.linkedin.com/in/akash-dhaigude-25ab952ab/" target="_blank">🔗 Linkedin</a>
        <a href="https://github.com/Akash1975" target="_blank">🔗 GitHub</a>
        <a href="mailto:akashdhaigude1975@gmail.com">📧 Gmail</a>
      </div>
      <small>&copy; 2025 Akash Dhaigude. All Rights Reserved.</small>
    </div>
  </footer>

  <script>
    const navToggle = document.getElementById('navToggle');
    const navList = document.getElementById('navList');
    const overlay = document.getElementById('overlay');

    navToggle.addEventListener('click', () => {
      navList.classList.toggle('show');
      overlay.classList.toggle('show');
    });

    overlay.addEventListener('click', () => {
      navList.classList.remove('show');
      overlay.classList.remove('show');
    });

    const popup = document.getElementById('popup');
    const popupTriggers = document.querySelectorAll('.popup-trigger');
    const closeBtn = document.querySelector('.close');

    popupTriggers.forEach(img => {
      img.addEventListener('click', () => {
        popup.style.display = 'block';
      });
    });

    closeBtn.addEventListener('click', () => {
      popup.style.display = 'none';
    });

    window.addEventListener('click', (e) => {
      if (e.target === popup) popup.style.display = 'none';
    });

  </script>
</body>

</html>